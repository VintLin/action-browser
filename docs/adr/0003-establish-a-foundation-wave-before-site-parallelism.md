# Establish a foundation wave before site parallelism

Site capability work may be delegated in parallel only after a Foundation Wave defines the normalized capability diff, canonical intent mapping, adapter output contract, command/document inventory checks, test template, and real-browser evidence format. This deliberately delays site implementation so independently executing models cannot invent incompatible command semantics, outputs, or acceptance criteria that would require broad rework later.
