# Check in one machine-readable capability catalog

The repository stores one JSON Catalog Source containing the Reference Baseline, Capability Records, evidence, statuses, scores, and exclusions. Human-readable Markdown matrices are generated Catalog Views and never edited independently; specs, tickets, inventory checks, and Maintenance Cycles all consume the same JSON so parallel models cannot create competing interpretations.
